version = point_system_version

addProjectDependency(":mods:metacraft-player-specific-scoreboards")

dependencies {
	implementation(include("org.xerial:sqlite-jdbc:$jdbc_sqlite_version"))
	implementation(include("org.postgresql:postgresql:$jdbc_postgresql_version"))
}