version = project.dungeons_version

addProjectDependency(":mods:metacraft-core")

addPolymerDependency()

dependencies {
	modCompileOnly("maven.modrinth:squaremap:${project.squaremap_version}")
}

test {
	useJUnitPlatform()
}