version = project.simple_custom_features_version

loom {
	accessWidenerPath = file("src/main/resources/simple-custom-features.accesswidener")
}

addPolymerDependency()

addProjectDependency(":mods:metacraft-zones")
addProjectDependency(":mods:portal-blocker")

dependencies {
	implementation include("net.bytebuddy:byte-buddy:${project.byte_buddy_version}")
}
