version = project.end_relay_version

addCoreDependencies()

loom {
	accessWidenerPath = file("src/main/resources/metacraft_relay.accesswidener")
}

setupRunDatagen("metacraft-relay")

dependencies {
	modImplementation("eu.pb4:polymer-virtual-entity:${project.polymer_version}")
}
