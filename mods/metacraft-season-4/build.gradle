version = project.season_4_version

addProjectDependency(":mods:metacraft-core")
addLeukocyteDependency()
addPolymerDependency()

dependencies {
	modCompileOnly("xyz.nucleoid:stimuli:${project.stimuli_version}")
}
