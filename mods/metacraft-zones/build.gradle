version = project.zones_version

addCoreDependencies()
addProjectDependency(":mods:metacraft-resource-packs")
addLeukocyteDependency()

loom {
	accessWidenerPath = file("src/main/resources/metacraft_zones.accesswidener")
}

dependencies {
	modCompileOnly("xyz.nucleoid:stimuli:${project.stimuli_version}")
}
